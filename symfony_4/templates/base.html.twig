<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        
        {% block javascripts %}{% endblock %}
    </head>
    <body>
        {% block body %}
            {% for i in 1..20 %}
                <div>
                    <h1> {{ 'Hola Mundo' | upper }} </h1>
                </div> 
            {% endfor %}

            {% if var == "mostrar" %}
                <div>El IF es verdadero y por lo tanto se muestra este contenido</div>
            {% endif %}

        {% endblock %}
    </body>
</html>
